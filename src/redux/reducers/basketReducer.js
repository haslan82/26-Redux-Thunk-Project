const initialState = {
    isLoading:false,
    error:false,
    data: [],
}
const basketReducer = (state= initialState, action)=> {
    // console.log(state);
    // console.log(action);
return state;
}
export default basketReducer